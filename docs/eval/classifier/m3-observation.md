# M3 history assessment observation

These are three standalone assessment runs, with no live decision-path or app integration claim. The event was built from public SCEN0004 authorization AU0035 and its supplied evaluation draft with `scripts/replay.py`'s event builder. The eight-second real-clock deadline was fresh for this offline call. The customer's own base-pack history supplied 50 numeric `hist-1` values. No raw customer, card, merchant or item identifier, description or merchant-authored text entered Jev's request state.

The shipped `assess_jev()` accepts a Jev-only key from the startup caller. It never reads the shared `.env`. For these one-off runs, `leash.runner.settings._parse_env` was the only code that read the shared main-checkout file; the caller passed only the OpenRouter entry in memory, without printing it. A live runner still needs an agreed settings-owned scoped accessor and a mandate lock and policy/state recheck after the provider returns.

The request went to OpenRouter System One with model `typesafe/jev-1.13-20260917`, provider restricted to TypeSafe and fallbacks disabled. It returned the same served model in 471 ms with the evaluation-only CatBoost score present, then in 482 ms with behaviour disabled. Both `spend_pattern` and `activity_pattern` selected `ordinary` in both calls. A third real call after changing the transport to stream the response returned in 476 ms, with a total history-plus-provider run of 1,389 ms and 6,610 ms left on the fresh eight-second deadline. It also selected `ordinary` twice and left behaviour disabled. The adapter validates the full two-question set, exact option keys, unique JSON keys, finite probabilities in [0,1] summing to one within 1e-6, local argmax agreement and uncertain exact ties. A tied provider choice may be null or one of the tied maxima; the local selection stays uncertain. The streamed response is bounded to 64,000 bytes during receipt, requires UTF-8, and is parsed within the provider timeout. At least two seconds must remain for a state recheck. A malformed response raises `JevResponseError`; a deadline or network failure propagates, with no substitute decision.

In the first run, the evaluation-only CatBoost artifact scored the event at 0.097382, model id `catboost`, artifact hash prefix `e4815c567707`, `escalation_fired=False`. That `AssessmentBundle` carried authorization AU0035, a purchase-and-feature digest prefix `5c0490bdce17`, policy hash prefix `3a8384040867`, event timestamp, history profile, behavioural score and semantic answers. `validate_bundle()` accepted it. The exact digest is local integrity evidence, not caller authentication.

No `evaluate(..., assessments=...)` call, model-driven step-up, customer answer, simulator request, app/API request or live deadline run occurred. P1/P2 and runner integration remain outside this standalone cut. These runs spent three provider requests; no test suite or linter was run.
