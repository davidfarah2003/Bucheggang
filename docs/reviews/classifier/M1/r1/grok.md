BLOCK

Target head `04cc78c8f70c425b17d5ded022cc45f77520a1c0` (merge of PR #126, parents `b14a56aef191775eb89e8820e70d23c3a6c8464a` and `32a4dd074777d5ad3a15f482eb1344955ca57fc0`). Base `b14a56aef191775eb89e8820e70d23c3a6c8464a` is the first parent and an ancestor. Both hashes were read before grading and again immediately before this report. `git status --porcelain` was empty at both checks. A changed head voids this verdict.

Model pin: Jcode session `session_skunk_1790326104780_1711cab9496d6932` records `model=grok-4.7`, `provider_key=cliproxy`, `reasoning_effort=high`. The persona requests `model: grok-4.7` and `variant: high`. The 2026-09-25 10:48:25 log records `set_reasoning_effort` with ENV_SNAPSHOT model `grok-4.7`. Requested effort high. Accepted effort high. This is the seat record, not a provider-side reasoning trace.

## Blocker

B1. Model-off startup builds a validated personal-history profile and then discards it. The decision and its evidence are identical to the pure engine, which never reads `HistoryIndex`.

`HistoryOnlyEvaluator.__call__` (`src/leash/engine/classifier/bridge.py:16-22`) requires facts, builds `history_bundle`, validates it, and passes the bundle to `evaluate`. `model_checks` (`src/leash/engine/model_checks.py:40-75`) emits a check only when `bundle.behaviour` or `bundle.semantic` is set. The history-only bundle sets both to `None` (`src/leash/engine/classifier/assess.py:21-26`). `evaluate` (`src/leash/engine/evaluate.py:109-111`) adds only those checks. Familiarity, country, lookalike, and `history.merchant_seen_on_card` / `history.device_seen_on_card` still read `leash.engine.data.History` (`src/leash/engine/checks.py:97-128`, `src/leash/engine/rules.py:187-198`), keyed by `authorization.card_id` and a timestamp cutoff, not by the mandate-bound `HistoryFeatures`.

Failure. On AU0001 (CU0001, CA0001, 2026-08-09 10:04:00+00:00) the bound profile has 50 numeric features: `card_merchant_approved_count=26` and `customer_merchant_approved_count=47`. The guarded `HistoryOnlyEvaluator` decision is `decline` with `merchant_type_mismatch|item_mismatch`. Its evidence names are exactly the pure-engine names. The serialized decision does not contain `card_approved_purchase_count` or the feature `customer_id`. Zero evidence checks have `source="model"`. A caller cannot show, from the decision, which customer history was bound or that a history read happened. A later change to feature values cannot change this path's outcome. The milestone asks for personal-history features to be exercised through the actual evaluator with observable outputs (`docs/plans/02-classifier-handoff.md:35`). This hookup does not do that.

The same run reproduced the owner's 45-row table: 11 approve, 32 decline, 2 step_up, every decision and reason string equal to `docs/eval/replay-2026-09-25-all.csv`, and also equal to `evaluate` without a bundle. That parity is the defect, not evidence that history was integrated. Credential caches stayed at hits 0, misses 0. No NumPy, CatBoost, or sklearn module was loaded.

## Checked, not blocking

Identity. `HistoryIndex.for_event` (`src/leash/engine/classifier/history.py:258-265`) rejects an authorization whose card or mandate id differs from the mandate, and a mandate card that does not belong to that customer. Reproduced: wrong customer on CA0001 raises `ValueError: AU0001: mandate card does not belong to customer`. Authorization card CA0002 against mandate card CA0001 raises `ValueError: AU0001: authorization and mandate identity differ`. A state whose `mandate_id` differs raises `ValueError: AU0001: assessment state belongs to another mandate` from `validate_assessments` (`src/leash/engine/model_checks.py:21-22`). Loaded packs had 0 rows whose customer was not the card owner, and 0 customer-index rows on a foreign card. Swapping both mandate customer and card to another real pair (CA0003, CU0002) is accepted, because the index then agrees with itself. Isolation depends on the caller supplying the customer's mandate, which the runner checks separately in `handle` (`src/leash/runner/loop.py:209-210`).

Cutoff. `_prior` (`history.py:163-170`) uses `bisect_left` on `(timestamp, authorization_id)`, so a row is excluded at its own key. Reproduced on CA0001's first row TR00197: prior count 0 and the row is not included. An id that sorts before that row at the same timestamp also excludes it. An id that sorts after includes it. The packs have 0 card-level and 0 customer-level same-timestamp groups, and 0 of the 45 public attempts share a history timestamp on the same card. Public attempt ids are not in the history key set.

Customer versus card scope is computed. CU0001 has CA0001 (122 rows) and CA0002 (116 rows), 238 customer rows. AU0001 features report card approved purchases 119 and customer approved purchases 229. Those customer-scope numbers are not read by any check.

Injection. `evaluate._quarantine` (`evaluate.py:31-37`) clears fact fields and sources when `contains_instructions` is set. Real AU0037 and AU0040 keep the injected check `uncertain` with source `merchant_text`. Forcing AU0001's line to `contains_instructions` with product type and `matches_request` still set yields quarantined `product_type=None`, empty sources, and `step_up` / `injected_instructions`. Feature values are numeric. Merchant text is not copied into them. Jev instructions (`jev.py:22-47`) are static and are not called on this path.

Failure handling. Missing pack path raises `FileNotFoundError` for `accounts.csv`. `HistoryIndex({})` raises `ValueError: history requires at least one explicit pack`. `facts=None` raises `ValueError: AU0001: extraction facts are missing`. A naive timestamp raises `ValueError: AU0001: timestamp lacks timezone`. `LEASH_ENABLE_MODELS=yes` raises `SettingsError: LEASH_ENABLE_MODELS must be 0 or 1`. Unset and `0` both return `models_enabled=False` (`settings.py:53-59`). `load_evaluator` then constructs `HistoryOnlyEvaluator(HistoryIndex())` (`evaluation.py:110-118`). There is no second return of the bare engine on that branch. CLI `run_loop` uses `book.evaluator` (`cli.py:78`), and `StepUpBook.__init__` calls `load_evaluator` (`stepups.py:100`), so the pending recheck uses the same class. Eight concurrent reads of one shared index on AU0001 produced no errors. This process did not mutate the index.

Model-off side effects. `assess.py:12` imports `assess_jev` at import time, so `leash.engine.classifier.jev` loads during model-off startup. The import does not call `load_openrouter` or `settings.load`. After `load_evaluator` and 45 guarded decisions, both caches were `CacheInfo(hits=0, misses=0, currsize=0)`. Direct `evaluate` with a forged `BehaviorAssessment(escalation_fired=True)` changes AU0012 from `approve` / `within_policy` to `step_up` / `model_history_uncertain`. `HistoryOnlyEvaluator` rebuilds the bundle and cannot take that object. It still returns `approve`. That forged path is not reachable through model-off startup.

Count gap, not a public-attempt decision change. `History.card_rows` counts every row before the timestamp (`data.py:94-96`). Classifier `card_prior_attempt_count` counts purchases only (`history.py:191-194`). AU0001 is 122 versus 121 because of refund TR02798. All 45 public attempts differ by the non-purchase rows on their card. Merchant-approved counts match on all 45. Twenty-eight cards have a cash withdrawal or refund before their first purchase, so an event in that gap would see engine history and a zero classifier purchase count. No public attempt is in that gap, and no card has only non-purchase rows. Because B1 already drops the classifier count, this gap does not independently change the 45 decisions.

`readiness.py:50-58` still describes a model-off dry run and calls `settings.load()`, which reads the simulator `.env`. It does not select the evaluator. Not a decision-path blocker. The CLI docstring (`cli.py:8-10`) still says the pure engine is the default. The code no longer does that.

## Commands and outcomes

- `git rev-parse HEAD` before and after: `04cc78c8f70c425b17d5ded022cc45f77520a1c0`. Base resolves and is an ancestor. Status empty both times.
- External interpreter `/Users/david/Projects/Bucheggang/.venv/bin/python` with `PYTHONPATH=src` and `PYTHONDONTWRITEBYTECODE=1`. One earlier `uv run` created `.venv` in this grading tree. That directory was removed. Status was empty afterward. No test file, suite, or linter was run. No provider, simulator, or customer-answer call.
- `load_evaluator()` with `LEASH_ENABLE_MODELS` unset returns `HistoryOnlyEvaluator`, 876 owners, 873 cards.
- Guarded `decide_with_guard` over the five drafts (`docs/samples/scen0002_draft.json` and `docs/eval/replay-policies/SCEN000{0,1,3,4}.json`): 45 rows, 11 approve, 32 decline, 2 step_up, max `elapsed_ms` 0, 0 diffs against `docs/eval/replay-2026-09-25-all.csv`, 0 model checks, credential caches 0/0.
- Identity, cutoff, missing-pack, bad flag, facts-none, state-mismatch, injection quarantine, forged-bundle, and concurrency probes produced the results cited above.

## Limits

No live runner, Wallet, bootstrap, or `/v1` call. No pending step-up file was written. The 45-row check uses the supplied evaluation drafts, not a customer confirmation store. Provider reasoning tokens were not inspected. The Jev import was not executed. Peer review reports were not used as evidence.
