# 05 Simulator runner

- Status: foundation, replay and original-theme reports merged in #31, #32 and #34; ownership and Activity repairs merged in #41 and #43. Deadline handling is merged in #50. Durable intents and customer coordination are merged in #58 (eb78312). The live-call gate is lifted (Log, 05:05); a dry run is scheduled for 10:30 and no live run has been recorded since the gate lifted.
- Owner: David (proposed)
- Lane: runner. Channel `team.zurichbuchegg.runner`, branch `lane/runner`, worktree `.worktrees/runner`
- User flow step: everything between "mandate confirmed" and "decision recorded": mandate calls, scenario start, polling, deadlines, decision submission, `/resolve`, state persistence, offline replay
- Design: [Hackathon demo mode](../idea/viseca-agent-control-layer.md#hackathon-demo-mode), [Demo constraints](../idea/viseca-agent-control-layer.md#demo-constraints)
- Papers: [SAFR](../papers/SAFR.pdf) (no unattended auto-approve on timeout; the audit log is written at the point of decision)
- Challenge API: [technical_details.md, steps 4 to 8 and the endpoint table](../../viseca-2026/technical_details.md); the existing [viseca_quickstart.ipynb](../../viseca_quickstart.ipynb) already does the connection check and is the starting point
- Contracts: `Event`, `Decision`, `MandateState`, `StepUp`, engine entry points in [contracts.md](../contracts.md)

## Goal

A worker that keeps a scenario run moving under the deadlines, with every accepted result written down once. It is the only code that holds the bearer key and the only code that talks to the simulator. It also provides `scripts/replay.py`, which feeds the 45 public attempts through the same `evaluate` call offline.

## Scope

In:

- `leash.runner.settings`: base URL and key from `.env`; `GET /v1/bootstrap` at start for the real deadline and human-window values.
- Mandate client: create, confirm, get, PATCH (tighten), DELETE (revoke). Called by the policy lane's confirm route and the app's tighten and revoke routes.
- Run loop: `POST /v1/scenario-runs`, then `GET /v1/decision-requests/next?wait=25` in a loop; 204 means check progress and continue; validate the envelope and the event strictly; skip a live ID already in `MandateState.handled` (reconcile, do not resubmit); otherwise call extract with a budget, then `evaluate`, then submit before `deadline_at`.
- Deadline guard: the budget for extract is `deadline_at - now - 2 s`, capped at 1.5 s. Evaluation must return 1 s before the deadline. Either timeout logs and raises without submitting a substitute decision. HTTP submission must finish, including its response body, by `deadline_at`.
- `step_up` handling: record as pending, expose through `GET /step-ups/pending`; when the app answers, `POST /resolve` and record; when the human window passes with no answer, `POST /resolve` with `decline` and `step_up_timeout`.
- State persistence after every accepted result (`data/state/<mandate_id>.json`), so a restarted worker reconciles instead of double-counting.
- Structured log per authorization: received, extract ms, evaluate ms, submitted at, accepted, time to deadline.
- `scripts/replay.py`: builds events from the CSVs in `replay_order`, assigns fresh real-clock deadlines, runs extract and `evaluate` with a given draft per scenario, prints the decision table and writes `docs/eval/replay-<date>.csv`.

Out:

- Deciding anything (plan 02). Reading merchant text (plan 03). Screens (plan 04).

## Steps

1. Settings and API client with a 30 s total HTTP budget, shortened by a supplied absolute operation deadline. No retry on 5xx or anywhere else: a non-2xx raises `ApiError(status, body)` (AGENTS.md section 6). Call `/healthz` and `/v1/bootstrap` once to see them answer.
2. Mandate client. Try it on the `SCEN0000` instruction (the quickstart's rule) once the key arrives. No recorded response substitutes for a live call.
3. `scripts/replay.py` with the `SCEN0002` fixture draft, then explicit evaluation drafts for all 45 public attempts. Reviewed and merged in #31 at `8cb591a`; commands and assumptions are in [run-replay.md](../run-replay.md).
4. Run loop and deadline guard. Try it against the live API as soon as the key arrives.
5. Step-up handling and timeout resolution.
6. Persistence, so a restarted worker does not count spend twice. Try it once by killing the worker mid-run.
7. Live runs use the team's bootstrap IDs for the original five themes: `SCEN0101`, `SCEN0135`, `SCEN0130`, `SCEN0106`, `SCEN0122`. All completed; reports are `docs/eval/live-<scenario>-2026-09-24.md`. `SCEN0000` to `SCEN0004` remain the public offline pack. The five additional live themes listed later by bootstrap are outside this verification scope.

## How we check it works

- Real-engine `SCEN0101` completed in #29. The other four original themes completed with the recorded results below. Offline `SCEN0000` to `SCEN0004` replay covers all 45 public attempts.
- `grep -rn "TEAM_API_KEY\|Bearer" src/` matches only `leash/runner/settings.py` and the client.
- A worker killed mid-run and restarted counts spend once.

## Recorded live verification

| Scenario | Generated | Delivered and locally handled | Finalized | Pending at exit | Result |
| --- | ---: | ---: | ---: | ---: | --- |
| SCEN0135 | 12 | 11 | 12 | 0 | One platform-only timeout during worker downtime; explicitly not a clean all-delivery pass |
| SCEN0130 | 13 | 13 | 13 | 0 | Pending-step-up kill/restart completed without duplicate initial submission |
| SCEN0106 | 12 | 12 | 12 | 0 | Completed |
| SCEN0122 | 13 | 13 | 13 | 0 | Completed |

These four runs finalized 50 attempts: 49 local declines and one platform-only timeout. No customer answers were sent and no purchase was approved. Missing history on the live cards kept otherwise compliant purchases uncertain, and unanswered step-ups declined after the real timeout. This verifies runner behavior, not purchase-decision accuracy or a successful customer approval journey.

Known limits: the household downtime timeout remains recorded; the pending-state restart check had zero approvals and therefore does not demonstrate preservation of nonzero approved spend; the accepted-submit-before-persistence gap is closed by the intents in #58; smoke evaluators and timeout substitute decisions are gone since #50. HTTP timeouts report unknown remote outcomes; durable intents and reconciliation are implemented in #58 and not yet exercised against the live simulator. A live revocation with requests already queued remains a Friday verification item. Model assessment contracts and pure composition merged in #47, but runtime models stay off after the [real-provider exercise](../eval/jev-public-2026-09-25.md) stopped on non-normalized probability distributions; model-enabled runner integration and an operating threshold remain unverified.

## Decisions

- Simulator runs retain their starting mandate snapshot; PATCH affects later simulator runs (technical_details.md:286,380). Local service policy applies at the time of each new decision, including tightening and revocation. An already accepted purchase is not re-decided. A pending step-up under a revoked mandate resolves to decline with `mandate_revoked`. Platform cancellation is reported only when the API confirms it. This records the engine-owner O3 semantics; customer-first locks and effective-policy rechecks merged in #58, while their live verification remains open.
- Reset is disabled on the live API; each demo run creates a fresh mandate and never reuses IDs.

## Log

(one line per finished task: date time, who, what, how it was tried, sha)
2026-09-24 19:47 runner_builder: settings, API client and mandate client (leash.runner.settings, .api, .mandates); tried with `uv run python -c` calling api.healthz() -> {"status":"ok","service":"saw26-sandbox","api_version":"0.1.0"}, api.bootstrap() -> team_id team2, limits decision_timeout_seconds 8, step_up_timeout_seconds 120, long_poll_max_seconds 25, features.reset false, live scenarios SCEN0101 SCEN0135 SCEN0130 SCEN0106 SCEN0122 (not SCEN0000..0004); mandates.create(SCEN0000 quickstart draft, rule billing_amount_chf <= 20 CHF purchase, uncertainty ask) -> draft_c108c8b3cdf66c29, confirm -> TMd8b7df71d23ca697, get -> status active; get("TMdoesnotexist") raised ApiError 404 mandate_not_found; key redacted, read only in settings.py, @e383b5d
2026-09-24 19:47 runner_builder: bootstrap says team reset is disabled (features.reset false), so the open question on POST /v1/team/reset before demo runs resolves to: create a fresh mandate per run instead.
2026-09-24 19:58 runner_builder: run loop and deadline guard (leash.runner.loop.run_loop(run_id, evaluate, state), start_run, poll, handle, decide_with_guard; CLI leash.runner.cli with smoke evaluator decline_everything_smoke, never used in the demo). Live: POST /v1/scenario-runs with SCEN0000 returned HTTP 500 because our team only has SCEN0101 and the other bootstrap IDs, and the SCEN0000 mandate TMd8b7df71d23ca697 read back "superseded" (409 mandate_not_current) after the reviewer created a newer one, so the team holds one active mandate at a time. Fresh mandate TM58df07bd7241697f. First run run_effe33ed32ae00ce hit strict validation: envelope event_id is an int, and the run record has flat *_event_count fields plus status, not a counters object; both events went to platform timeout (decision_source timeout, reason timeout). Fixed; `uv run python -m leash.runner.cli --scenario SCEN0101 --mandate-id TM58df07bd7241697f --evaluate decline_everything_smoke` -> run_153aed19cb9ec643, AU10001 and AU10002 submitted about 8.0 s before deadline_at, accepted status declined with decision_source team, 204 then run status completed, 2/2 finalized. Guard tried offline on a recorded live event with deadline_at now+2 s and a 3 s evaluator: step_up engine_timeout returned after 1.01 s. The evaluator thread keeps running after the guard fires, so a hung evaluate holds the single worker and later requests also time out to engine_timeout. Key redacted, @d52f6e2
2026-09-24 20:05 runner_builder: live mandate client shapes, tried on throwaway mandates (all revoked after). get returns the raw mandate with no wrapper and no version field: draft_id, mandate_id, status, instruction, hard_rules (null optional keys), uncertainty_policy, guidance, open_questions, created_at. PATCH {"uncertainty_policy":"decline"} returns the same shape. PATCH {} returns 422 empty_update. Every PATCH with hard_rules returned 409 mandate_widening: unchanged list, the list as first submitted, integer value sent as 20.0, and an appended rule placed last or first. Rule append through tighten therefore does not work live yet. revoke returns 2xx, and a second revoke is also 2xx. Confirming a new mandate supersedes the previous one, so these tries left no active mandate for the team.
2026-09-24 20:08 runner_builder: extract wired into handle(): after validation and the handled check, facts = leash.extract.extract_event(event, requested=requested_item(draft)) under min(1.5 s, deadline_at - now - 2 s). requested comes from the confirmed draft's "=" rules on facts.product_type and facts.size. The facts go to evaluate(event, policy, state, facts), the docs/contracts.md "Engine entry points" signature. If extract misses its budget the loop declines with engine_timeout and does not call evaluate; an exception from extract propagates. The CLI now takes --draft (confirmed PolicyDraft JSON), and the loop raises if the event's mandate instruction differs from the draft's. Live: `uv run python -m leash.runner.cli --scenario SCEN0101 --mandate-id TM1eac78716fe09fab --draft <scratch>/scen0101_draft.json --evaluate decline_everything_smoke` (draft made with DraftStore.create from the SCEN0101 instruction and rule billing_amount_chf <= 20) -> run_e2f113c00e002eea, AU10001 and AU10002 extract_ms 0, submitted 8.0 s before deadline_at, accepted declined, decision_source team, run completed 2/2. Offline on a recorded live event with the SCEN0002 sample draft: requested {product_type: road-running shoes, size: 43}, facts reached evaluate unchanged, and an extract slowed to 3 s declined with engine_timeout after 1.51 s without calling evaluate. @d958f33
2026-09-24 20:32 runner_builder: step-up handling (leash.runner.stepups.StepUpBook, leash.runner.routes.step_up_router(book, authenticated_customer) with GET /step-ups/pending and POST /step-ups/{authorization_id}/answer). An accepted step_up becomes a pending StepUp with expires_at = accepted time + bootstrap step_up_timeout_seconds (120) - 5 s. The answer route sends the customer's approve or decline to POST /v1/authorizations/{id}/resolve and records it. A sweeper thread resolves each expired step-up with decline, and our Decision carries step_up_timeout. The run loop ends only when the run is finished and nothing is pending. Findings from the live API: the platform redelivers a pending step-up on every poll and holds the next request back until it resolves, so the first try logged 736 redeliveries in 10 s; the loop now pauses 1 s on a pending redelivery and does not log it. /resolve refuses reason_codes (422 extra_forbidden, only decision, customer_message and evidence are allowed), and it stores reason_codes ["human_confirmation"] for both an approve and a timeout decline. The platform's own expiry after 120 s is decline with reason step_up_expired. Two aborted runs (run_5e1c5e693c0c308d, run_d36792338db2f0d3) ended with platform step_up_expired and timeout. Clean live try: `uv run python -m leash.runner.cli --scenario SCEN0101 --mandate-id TM1eac78716fe09fab --draft <scratch>/scen0101_draft.json --evaluate step_up_everything_smoke --serve-port 8791` -> run_af706835b5e31ccb. AU10001 step_up, then curl POST /step-ups/AU10001-b5e31ccb/answer approve -> HTTP 200, status approved, resolved_by human, state.approvals gained 18.0 CHF. AU10002 step_up at 18:29:32 left unanswered; the sweeper resolved it at 18:31:27 -> status declined. Run completed 2/2 and the loop exited. The interim in-memory record stays until contracts #21 merges. @0fa7ec2
2026-09-24 20:55 runner_builder: persistence and history routes (task 6). The run loop reads leash.engine.state.load for every request and records each accepted submit and /resolve through leash.runner.records.record_accepted, which calls leash.engine.state.record (sole writer of data/state/<mandate_id>.json) and then writes data/decisions/<mandate_id>/<authorization_id>.json (a resolved step_up adds <authorization_id>.resolve.json), each { decision, event, state_before, state_after, accepted_at, accepted }, exclusive create, only after the API accepted. StepUpBook is file-backed at data/stepups/<mandate_id>/<authorization_id>.json (pending, then resolved, atomic rename); every write for a mandate runs under flock data/locks/<mandate_id>.lock, so the loop process and the API process share it. leash.runner.routes.history_router(authenticated_customer) serves GET /mandates/{mandate_id}/decisions -> [{decision, state_after}] ordered by accepted_at, and GET /decisions/{authorization_id} -> {decision, event, state_before, state_after} (the /resolve record when one exists); 404 when unknown. The CLI raises if decisions exist for a mandate but its state file is missing. Live on fresh mandate TM86c7efdc7b031e3a: (1) decline_everything_smoke run_79338b9ab471b77f, worker killed with -9 after both submits (the loop handles both in 30 ms, so it could not be killed between them), restart with --run-id printed state at start handled [AU10001-b471b77f, AU10002-b471b77f] and exited on 204 completed with state declined 2, approvals 0. (2) step_up_everything_smoke run_5b4c0b025db068a3, killed -9 while AU10001-5db068a3 was pending; restart logged skipped_handled step_up for it and did not resubmit; a TestClient in a separate process listed GET /step-ups/pending -> [AU10001-5db068a3] and POST answer approve -> 200 {status approved, decision_source human, resolved_by human}; the restarted loop then got AU10002, its sweeper resolved it at 20:53:38 decline step_up_timeout (simulator status declined), run completed and the loop exited. Final state approvals [AU10001-5db068a3 18.0 CHF] (counted once), declined 3, pending [], handled 4. TestClient: GET /mandates/TM86c7efdc7b031e3a/decisions 200 with 5 entries oldest first (2 declines, step_up, approve, step_up), GET /decisions/AU10001-5db068a3 200 approve with state_before approvals 0 pending [AU10001-5db068a3] and state_after approvals 1 pending [], unknown mandate and unknown ID 404. Known gap: a crash between an accepted submit and record_accepted leaves the ID out of state; the restart would resubmit it and the simulator's answer decides. @ead0d21
2026-09-24 21:44 runner_builder: step 7 with the real engine. leash.runner.cli EVALUATORS gains "engine" (leash.engine.evaluate.evaluate); the two smoke evaluators stay. Fresh mandate TMed393c21a6c87d01 from DraftStore.create on the SCEN0101 instruction (rule billing_amount_chf <= 20 CHF, ask) confirmed through leash.policy.confirmation.confirm_policy. `uv run python -m leash.runner.cli --scenario SCEN0101 --mandate-id TMed393c21a6c87d01 --draft <scratch>/scen0101_draft.json --evaluate engine --serve-port 8791` -> run_a883192a054a6746 completed 2/2: AU10001 step_up [new_device, country_blocked], answered approve by curl -> approved, decision_source human; AU10002 decline [amount_over_limit], accepted declined. Engine card history comes from the organizer pack, which has no rows for live card CA1331, so live purchases are uncertain on device and country. Decision log docs/eval/live-SCEN0101-2026-09-24.md. @778cd6d
2026-09-24 22:04 david_orch: step 3, scripts/replay.py and docs/run-replay.md, @1dfa0e1. `uv run python scripts/replay.py --policy SCEN0002=docs/samples/scen0002_draft.json` evaluated 12 attempts: sequential approve 1/decline 11; independent mode approve 2/decline 8/step_up 2. `--all` with the four explicit unconfirmed drafts under docs/eval/replay-policies plus the shoes draft evaluated all 45 rows: approve 11/decline 32/step_up 2. Re-ran all 45 after rebasing on 7ccd7d7: same decisions, extract+evaluate p50 0.240 ms, p99/max 0.389 ms. No .env, simulator calls, persistent mandate state or customer answers. Missing --all policies, a zero deadline and existing output each failed with exit 2; existing report unchanged. CSVs retain evidence and timing. The monitor draft preserves the literal instruction while the extractor returns a different catalogue name; this produces item_mismatch and is documented, not silently corrected. Review pending.
2026-09-24 22:28 david_orch: restart repair @2ec640a. Live SCEN0135 run_9328cc3adcf476d9 left AU10371-dcf476d9 and AU10372-dcf476d9 locally pending after the platform expired them during worker downtime. Original sweep failed on HTTP 409 authorization_not_pending. StepUpBook.sweep(mandate_id, run_id) now handles only that response by reading the run-filtered GET /v1/authorizations list and verifying the exact finalized timeout-decline record before persisting it. Tried on those same authorizations: two POST 409 / GET 200 sequences, two observed timeout declines recorded, pending 2 to 0, approvals stayed 0; second sweep returned [] with no calls or duplicate records. No customer answers. Direct GET /v1/authorizations/{id} was tried and returned 404; shipped code uses only the supported list route. CLI passes run_id to the sweeper. Full evidence and remaining limits: docs/eval/restart-reconcile-2026-09-24.md. All 45 offline attempts re-run on the current engine: 11 approve, 32 decline, 2 step_up, unchanged; total p50 0.245 ms, p99/max 0.424 ms. Remaining live scenarios await this repair's review.
2026-09-24 23:41 david_orch: remaining original-theme live verification completed. SCEN0135 run_9328cc3adcf476d9 finalized 12, delivered/local 11, one platform-only timeout during repair downtime; SCEN0130 run_f3190df0414975d9 finalized/delivered/local 13 with a pending-state kill/restart and no duplicate initial submission; SCEN0106 run_4052aca422e9132a finalized/delivered/local 12; SCEN0122 run_3f7fdf8afb2858da finalized/delivered/local 13. All pending lists empty, all platform_rejected counts zero. Driver exited 0 and final worker exit verified. Authoritative run/authorization GETs matched the reports and CSVs. No customer answers, reset or live approvals. Four exact evaluation policies are recorded beside the reports. Reports are transport/state evidence, not an accuracy benchmark. Earlier checkpoints 2f41943, cfaabf8, 8f7dcf1; final report review pending.

2026-09-25 00:28 david_orch: customer ownership repair @0c6152e. Real HTTP against actual completed-run records reproduced an unrelated logged-in user getting 200 for mandate history and decision detail while GET mandate returned404. Shared confirmation lookup now gates history/detail/answer and filters pending by confirmed_by; rerunning the same HTTP reads produced404 for all three, anonymous requests remained401 and pending was empty. StepUpBook.get validated an actual stored step-up identity. No customer answer or simulator mutation. Standalone --serve-port without LEASH_POLICY_STORE failed before starting a run, exit1. Full 45-attempt replay at base4988981 remained11 approve/32 decline/2 step_up, local total p50 0.249ms, p99/max0.583ms. docs/eval/local-app-e2e-2026-09-25.md also records real MCP stdio/HTTP and 400px browser checks, the separate draft-link docs defect, and remaining positive-owner/human-flow limits. Review pending.

2026-09-25 00:34 david_orch: Activity history repair @e6fb2ff. The actual hiking-boots record directory gave19 rows for13 authorization IDs, with step_up rows opening final-decline detail. mandate_history now selects each accepted resolution over its initial submission and keeps both audit files; actual saved records across four completed runs gave84 stored records,49 unique Activity rows, and every decision/state_after matched final detail. An unused ID with no decision/state files returned[] without creating financial state. Missing history with nonempty state raises; reading holds the recording lock. Route ownership still runs before this reader. Full public replay exited0 with11 approve/32 decline/2 step_up; p50 total0.245ms and p99/max0.444ms. No API mutation or invented answer. The newly confirmed-owner HTTP case remains for the human-ready Wallet run. Evidence: docs/eval/activity-history-2026-09-25.md.

2026-09-25 david_orch: deadline-failure implementation on lane/runner-deadlines removes both fixed-output evaluators and timeout substitute decisions. Full public replay through scripts/replay.py and a separate pass through the actual runner extraction/evaluation guards both returned45 rows,11 approve/32 decline/2 step_up. Local replay total p50 0.227ms, p99/max0.420ms. Expired extraction/evaluation/submit/resolve raised before dispatch and without loading simulator settings; occupied worker extraction raised instead of returning missing facts. Real loopback HTTP returned200 and409 as expected; a trickled body exceeded a0.250s total budget and raised ApiTimeout after0.253s, with no retry. Scoped provider settings expose only a repr-hidden api_key. No simulator or model request, human answer, test suite or linter. Evidence: docs/eval/runner-deadlines-2026-09-25.md. Review and live credential rotation remain pending.

2026-09-25 david_orch: P3 storage checkpoint on lane/runner-intents adds private fsynced mutation intents, idempotent accepted-result recovery and bounded sorted mandate locks; not yet wired into dispatch. Replayed a genuine saved CHF18 accepted receipt in isolated storage, exited a separate process after state.record with code17, then recovered one approval and one history file without duplicate spend. Cross-process flock contention raised after0.103s for a0.100s budget and released earlier locks. Wrong response identity/status raised; intent permissions0600 and ignored by git. No live call or new answer. Evidence: docs/eval/runner-intents-2026-09-25.md.

2026-09-25 david_orch: integration scaffolding checkpoint adds current-policy lookup, coordinator recovery/recording, a durable dispatched phase that refuses repeat dispatch, and authoritative read-only reconciliation including verified platform timeout declines. Imports succeeded after rebasing onto merged customer_approvals contract #49 at8f640b7. These helpers are not yet connected to runner.loop, step-up answers or app mutation routes; no live calls were made and the new reconciliation paths still need execution evidence. The confirmed-customer runtime gates remain open.

2026-09-25 david_orch: connected runner submit, step-up resolve, app tighten/revoke and startup recovery to the durable coordinator. Final decision and customer-approval paths load customer_mandates from the confirmed owner under sorted locks; persistent before/after state stays mandate-local. All changed modules import. Full coordinator recovered the actual saved CHF18 receipt after a separate-process exit17 into one approval/history/resolved-step-up/journal, with no new answer or invented ownership. Real local HTTP returned401 anonymously,200 empty owned lists,404 for unowned reads/revoke,200 Wallet HTML,204 logout then401; simulator settings stayed unloaded. Missing confirmation store exited1 before remote startup. Public replay45 rows11/32/2, p50 total0.234ms p99/max0.388ms. Not release-ready: live calls, positive-owner flow, #54 helper compatibility and concurrent confirmation ordering remain open.

2026-09-25 david_orch: adopted merged #54 _effective(...,record), preserved global metadata and full global rules in tighten payloads, and added the agreed customer guard before owned-set enumeration. Shared-lock state reads use the owned set; persistent snapshots strip derived customer_approvals. A real separate process holding an empty customer set blocked the same customer for0.104s under0.100s while another customer acquired independently; an80-character Unicode guard worked with69-byte private filenames. Actual global-policy HTTP persisted/read back version1, rejected stale409 and duplicate422, and isolated another customer. Unicode global-policy GET500 was reproduced and reported to its owner, not hidden. No simulator call or invented confirmation.

2026-09-25 david_orch: repeated full replay after #54/customer-guard integration at5794faa:45 rows11/32/2, p50 total0.220ms p99/max0.519ms; repeated genuine-receipt separate-process exit17 recovery retained one CHF18 approval/history and completed step-up/journal. Customer-first ordering now covers mandate list/detail reads too. Actual HTTP encoded glob IDs returned422 before lookup, valid unknown IDs404, owned empty lists200; no simulator settings or customer answer. The strict records identifier allowlist was already in #58 and route validation now reports invalid identifiers as client errors.

2026-09-25 david_orch: rebased #58 onto main0e73499 with #47/#50/#65 merged, dropping the already-applied #50 commit. At code5ad01c2, replay45 rows11/32/2 (p50 total0.232ms, p99/max0.517ms), genuine-receipt process-exit recovery and Unicode/global-policy HTTP passed. Glob IDs returned422 before lookup. Runtime models remain off; live simulator and real human flow still blocked by credential rotation/availability.
2026-09-25 05:05 david_main: live-call gate lifted. Oskar (key owner) reports the organizer TEAM_API_KEY is public and cannot be rotated, so the "rotated" precondition set after the exposure is void. Live simulator runs may resume; the key stays only in the demo host's ignored .env, read by leash.runner.settings, and never appears in a message, log, persona or file. Posted on the spine at 05:05.
2026-09-25 05:07 david_main: merged eval #70 @4ed9962 over review_contracts12's BLOCK. The reviewer confirmed provenance, byte-identical measurements, probability lines, no success claim, no credential and replay 11/32/2; its blocker was the checklist line 61 wording, which is accurate at that head (#58 merged, live verification open, confirmation and PUT /global-policy still need the customer lock per #73's body). Owner ruling recorded on team.zurichbuchegg.review 05:07.

2026-09-25 david_orch: real local identity preflight against frozen #73 head4e4a5e5 via .cotal/identity_http_exercise_v3.py exited0. Actual Uvicorn/HTTP and MCP stdio covered36 recorded HTTP statuses plus concurrent registration201/409, session/global-policy persistence across API restart, Origin403, unpaired authoring and unapproved completion refusal, and login401x5 then429. Eight identity JSON files were0600 and checked supplied secrets were absent in clear. Two earlier probes stopped on a malformed UUID expectation and old MCP result field names; corrected probes required no application patch. No pairing approval, mandate confirmation, customer answer, simulator or provider call. Owned server stopped and disposable store removed. Evidence: docs/eval/identity-http-2026-09-25.md and .json. Separate archived-state check at merged #72 reached genuine-confirmation refusal instead of the old early deadline error; a complete handled-return path was not claimed without a genuine confirmation store.

2026-09-25 david_orch: real HTTP follow-up on frozen #76 head7803bd0 via .cotal/policy_locks_http_exercise.py exited0. Register/login without Origin or with a foreign Origin returned403; exact Origin returned201/200. Rejected login Origins left the existing session file byte-identical. Holding the actual account-A customer guard in a separate process left its global PUT unfinished while account-B PUT completed200; release let A finish200. Both accounts had no mandates. Concurrent same-version PUTs returned200/409. No confirmation, customer answer, simulator or provider call. Owned API/store cleaned. Evidence: docs/eval/policy-locks-http-2026-09-25.md and .json; nonempty ownership and real confirmation ordering remain for the live run.

2026-09-25 david_orch: effective-policy refresh now passes the stored hash_version and boundary_cases to draft_hash, preserving the v2 binding instead of producing a legacy digest. uv run python with a disposable LEASH_POLICY_STORE and actual DraftStore.create/get reproduced the old mismatch (v2 4eb0313f versus legacy f1330301), then executed the hash-call expression extracted from refresh with the pure draft_hash function: unchanged-rule v2 matched its stored hash, tightened-rule v2 matched an independent canonical SHA-256 calculation, and authored cases stayed unchanged in memory and on disk. V1 matched the exact legacy payload bytes. Two setup attempts stopped on an incorrect extractor signature and explicit null optional rule fields; corrected exercise used the actual mapping interface and valid proposal serialization. No full remote refresh, confirmation, provider or simulator call was made; temporary store removed. scripts/replay.py --all with the five docs/run-replay.md policies exited0:45 rows11 approve/32 decline/2 step_up, all decisions and reason strings identical to the committed CSV. Files limited to policy_context.py and this Log; #101 remains separate and frozen.

2026-09-25 david_orch: independent actual MCP transport exercise at policy #103 sourcefabd0a4. uv run python launched14 real stdio/HTTP startup processes with missing or malformed Wallet origins; all exited1 with origin errors. Real MCP2.2 clients then initialized over stdio and loopback HTTP and called begin_pairing: configured Wallet origin, code-only query, verifier absent from URL. HTTP Wallet origin differed from the MCP request origin. Unapproved completion returned PairingPending and authoring stayed denied; clear codes/verifiers absent from identity JSON. Owned processes stopped (HTTP SIGTERM exit-15), temporary stores removed. No Wallet approval, agent token, confirmation, customer answer, simulator or provider request. Evidence: docs/eval/pairing-origin-2026-09-25.md and .json.
