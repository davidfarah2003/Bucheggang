# 01 Policy draft and confirmation

- Status: draft
- Owner: Oskar (proposed)
- Lane: policy. Channel `team.zurichbuchegg.policy`, branch `lane/policy`, worktree `.worktrees/policy`
- User flow step: customer request, then draft policy, then confirmation in the Viseca app, then `mandate_id`
- Design: [Secure policy confirmation](../idea/viseca-agent-control-layer.md#secure-policy-confirmation), [Policy model](../idea/viseca-agent-control-layer.md#policy-model)
- Papers: [AutoCedar](../papers/2607.03656v1.pdf) (reviewed intent atoms, floors and ceilings, gaps surfaced before the policy is final), [SAFR](../papers/SAFR.pdf) (audit record of the confirmation), [Progent](https://arxiv.org/abs/2504.11703) (symbolic permission rules and monotonic confinement)
- Challenge API: [technical_details.md, step 5 and Rule format](../../viseca-2026/technical_details.md)
- Contracts: `PolicyDraft`, `Rule`, the field vocabulary, `POST /drafts/{id}/confirm` in [contracts.md](../contracts.md)

## Goal

Turn a cardholder instruction into a policy the customer can read, check against example purchases, and confirm. The shopping agent only ever receives the `mandate_id`.

The authoring flow works for any shopping category. The shoe scenario below is a sample for review, not a template or an allowed-category list. Unsupported product details remain visible as open questions until the customer reviews an enforceable draft.

Running example, from `SCEN0002`:

> Replace my worn road-running shoes in size 43. Buy only from a specialist sports retailer, only if the order can be returned within 14 days or more, and pay no more than CHF 200. Ask me when uncertain.

## Scope

In:

- User-side policy recommendation: the customer's agent gets the field vocabulary, rule format, examples, and known gaps from the MCP tool, then proposes rules from the cardholder instruction. The MCP backend validates and stores that proposal; it makes no LLM call.
- `propose_task_policy`: the user-side agent supplies the instruction and an explicit proposal. The backend validates its rule fields, source phrases, examples and open questions, then stores a canonical `PolicyDraft`. An invalid proposal raises.
- Progent's monotonic confinement rule applies to later policy edits: an expanded set of allowed purchases requires a fresh customer confirmation. Automatic tightening may only remove purchases from that set, and still needs an auditable version and hash.
- Draft store with version and hash. The app reads a draft from the backend by `draft_id`; it never gets one from the agent.
- The confirm route: hash and version check, answers to open questions folded into rules or `uncertainty_policy`, then the runner's mandate create and confirm calls, then `Mandate` stored and returned.
- The policy MCP tools supply authoring instructions and store proposed drafts. Status, purchase and runner tools can be added around the same confirmed mandate contract. No confirm, resolve, tighten or revoke tool is exposed to the shopping agent.

Out:

- Purchase decisions and step-up handling (plans 02, 05).
- Real Viseca authentication. The demo app uses a local login.
- Global policy management is parked until the live runner loop, app step-up flow, and replay work end to end. The intended shape is recorded under Later in plan 04.

## Steps

1. Draft schema in `src/leash/contracts/` with the engine lane (already sketched in `contracts.md`). A sample draft for `SCEN0002` in `docs/samples/` so the app lane can build against it today.
2. Provide the user-side agent a fixed instruction bundle with the field vocabulary, JSON shape, examples, and ambiguity prompts. Validate its proposed JSON on the backend; reject unknown fields and fields outside the vocabulary. Rule values stay numbers, strings or lists of strings. The backend makes no model call.
3. Agent-authored `examples[]` remain illustrative claims. Optional `boundary_cases[]` carry complete synthetic Event, PurchaseFacts, MandateState and frozen History inputs. Run every case through `leash.engine.evaluate` at proposal, revision and again before confirmation; reject an expected/observed mismatch. Persist computed outcomes separately from the v2-hashed cases and keep v1 hashes byte-for-byte unchanged. The Wallet reads customer-owned results from `GET /drafts/{draft_id}/boundary-results`; these synthetic outcomes are not live purchase evidence.
4. Open questions for gaps the instruction leaves, for example "the merchant does not state a return period: ask me, or decline?" The answer becomes a rule or sets `uncertainty_policy`.
5. Store the draft immutably with version and SHA-256 of its canonical JSON. Any edit is a new version.
6. Confirm route per `contracts.md`: mismatch is a 409; call the runner's mandate client and store the `Mandate`.
7. MCP server exposing the agent-facing tools, backed by the same store. A short transcript of an agent using them is a demo asset (plan 06).
8. Audit entries for draft, confirmation and rejection: what was submitted, version, hash, who confirmed, when.
9. `get_policy_status(draft_id)` tool. Reads the store and returns the draft's state: `pending`, `confirmed` with the `mandate_id`, `confirmed_at` and the confirmed version and hash, or `rejected` with the reason. This is how the agent gets its `mandate_id` after the customer confirms in the Wallet (design, "Secure policy confirmation", step 7). It reads `confirmation.json` and `rejection.json` through `DraftStore.get_confirmation` and a matching `get_rejection`; it never writes and cannot confirm. Unknown draft raises. Try it once: propose, poll `pending`, confirm in the Wallet, poll again and see the `mandate_id`.
10. `get_policy_summary(draft_id)` tool. Returns the current draft version's `plain_english` sentences, the examples with their expected outcomes, the open questions with answers where the customer has given them, and the `uncertainty_policy`. It is the read-back the design lists so the agent can show the customer what it proposed; the Wallet still loads the draft from the backend, never from the agent. No rule fields, no hash.
11. `docs/mcp-client.md`: the launch command (`uv run python -m leash.policy.mcp_server` with `LEASH_POLICY_STORE` set to the same directory as the Wallet API), a client config block for one MCP-compatible agent, and the tool list with one-line descriptions. Then record one real session as `docs/samples/mcp-transcript-scen0002.md`: the agent calls `get_policy_authoring_instructions`, proposes the SCEN0002 policy with `propose_task_policy`, polls `get_policy_status` until the Wallet confirms, and prints the `mandate_id`. That transcript is the plan 06 asset from step 7.
12. `buy(mandate_id, facts: [PurchaseFacts])` and `get_purchase_status(authorization_id)`. Parked for the 12:00 submission; see Decisions. The tool shapes are in `docs/contracts.md` under "MCP tools" so an agent can be written against them, and the backend answers `buy` with an error that says purchases arrive through the simulator in demo mode.

## How we check it works

- For each of the 5 scenario instructions, the extractor produces a draft that passes validation. A team member reviews each draft by hand against the instruction and notes the result in the Log.
- The `SCEN0002` examples produce approve, decline and step_up from `evaluate`.
- Changing a rule after the app has loaded the draft makes confirmation fail with a 409.
- `grep -r "confirm\|resolve\|revoke" src/leash/policy/mcp*` shows no agent-callable tool with those names.
- `list_tools` on the MCP server shows exactly `get_policy_authoring_instructions`, `propose_task_policy`, `get_policy_status`, `get_policy_summary`, `buy` and `get_purchase_status`, and every tool's input and output matches "MCP tools" in `docs/contracts.md`.
- After a Wallet confirmation, `get_policy_status` returns the same `mandate_id` the confirm route returned. Before it, `pending`. After a rejection, `rejected` with the reason.
- `docs/samples/mcp-transcript-scen0002.md` is a real session, with the draft id and mandate id it produced, and the same draft id appears in the store.

## Decisions

- The customer answers open questions on the confirmation screen. The app never chooses a default on the customer's behalf.
- Keep the simulator's `guidance` and `open_questions` fields empty because live events omit them. Store the customer's answers in the confirmed policy and audit record.
- 2026-09-24 23:30, David: the agent-facing MCP surface is the six tools in "MCP tools" in `docs/contracts.md`. The four authoring and status tools (steps 2, 9, 10) are in scope for Friday 12:00. `buy` and `get_purchase_status` (step 12) are declared in the contract and parked: in the challenge, purchases are generated by the simulator and reach the engine through the runner (plan 05), so an external agent has nothing to buy against before the live run, and the demo in plan 06 does not call `buy`. After the submission, `buy` builds an `Event` from the agent's cart and the confirmed mandate, runs `leash.extract.extract_event` over the agent's form for provenance, calls `leash.engine.evaluate` with `leash.engine.state.load`, records through `leash.engine.state.record`, and returns the `Decision`; a `step_up` goes to the Wallet through the existing step-up book and `get_purchase_status` reports the resolution. Until then `buy` raises with the message in the contract. No tool ever confirms, resolves, tightens or revokes.
- `get_policy_status` tells the agent only what the store knows: the state, the mandate id and the timestamps. It returns no rule text, so the agent cannot use it to learn the policy it was not shown, and `get_policy_summary` returns only the `plain_english` sentences the customer also sees.
- 2026-09-24 23:35, owner ruling from the spine: the policy MCP server gets an opt-in hosted transport. `python -m leash.policy.mcp_server --transport streamable-http --port <n>` runs `MCPServer.run(transport="streamable-http")` from the mcp 2.x library; stdio stays the default with no flags. One shared bearer token comes from `LEASH_MCP_TOKEN` at startup, a missing token raises, and a request without `Authorization: Bearer <token>` gets 401 from an ASGI middleware around `streamable_http_app()`. The server keeps the shared file-backed `LEASH_POLICY_STORE`. Demo-day hosting is David's machine behind a cloudflared quick tunnel. No OAuth, no cloud deployment, no per-user auth, no built-in Harness, no provider selection, no global or list UI. Claude Code connects through a `headersHelper` that reads the token from the environment, so the token is not written to any config (`docs/mcp-client.md`).
- 2026-09-25 00:20, Oskar, owner ruling by DM: extraction stays three-valued and never infers `facts.is_addon = false` from silence. The authoring guide tells agents not to emit `facts.is_addon = false` when the instruction forbids add-ons, because the engine's built-in add-ons check already fails a detected add-on line. Agents author no `facts.is_addon` rule at all for now: `engine.checks.addons()` blocks detected add-ons globally, so an allowed-add-on constraint could not be honoured without an engine change, and unknown stays unknown.
- The design's `request_policy_confirmation` is not a tool. Confirmation is requested by the agent handing the customer the Wallet link `/app/?draft_id=<draft_id>`; the backend never pushes to the customer. `get_user_context` is not a tool either: the agent gets the field vocabulary from the authoring guide and nothing about the customer's history or cards.

## Log

(one line per finished task: date time, who, what, how it was tried, sha)

2026-09-24 17:09 oskar1: SCEN0002 sample draft with six rules and five boundary examples; loaded `docs/samples/scen0002_draft.json` and recomputed its SHA-256, `hash_matches True`, @4b912dc.
2026-09-24 17:43 oskar1: immutable draft store and simulator draft mapping; created, revised and confirmed a temporary grocery draft through `DraftStore`, observed stale version rejected, version 2, simulator draft `SD-1`, mandate `TM-2`, @8b6d3b8.
2026-09-24 18:40 oskar1: serialized draft revision, confirmation and rejection with a per-draft file lock and checked rule value types; ran one temporary-store smoke flow, observed numeric string rejected, version 2, and one terminal race winner with one conflict, @6c90c83.
2026-09-24 17:45 oskar1: fixed-output prompt, strict model proposal validation and deterministic instruction compiler; ran five supplied instructions and observed 4, 3, 6, 3, 5 rules, while invalid model output raised `InvalidDraft`, @6837a79.
2026-09-24 17:52 oskar1: made compiler mode explicit under AGENTS.md no-fallback rule; reran five instructions with deterministic mode and got 4, 3, 6, 3, 5 rules; missing or invalid model mode output raised `InvalidDraft`, @09ae26f.
2026-09-24 18:00 oskar1: incorporated Progent's rule that policy expansion needs explicit approval and clarified that the customer-side agent drafts proposals while the MCP backend only provides instructions and validates/stores them; paper reviewed at arXiv:2504.11703v3.
2026-09-24 19:15 oskar1: implemented the agreed `confirming_answers` question contract in compiler output and draft validation; created a temporary SCEN0002 draft and rejected an answer outside its options, with six rules and two questions.
2026-09-24 19:22 oskar1: confirmation accepts only the draft's confirming answers and rejection checks the supplied version and hash; called SCEN0002 confirm and stale reject through FastAPI TestClient, observed 200 with a mandate ID and 409, using the local FastAPI dependency directory.
2026-09-24 19:32 oskar1: assembled the FastAPI customer shell with local username sessions and the policy draft router; called login, draft GET, app GET and logout through TestClient using the approved app static mount, observed 401, 200, 200, 204, then 401.
2026-09-24 19:47 oskar1: corrected missing and revoked session handling to return 401; called DELETE /session without a cookie, after login, and after logout, observed 401, 204 and 401.
2026-09-24 21:00 oskar1: integrated shell smoke against runner #25 @fb57cfc, mandate routes #26 @32abc7b and app static @448a6d6 in a temporary source tree; FastAPI TestClient observed login/session 200, unauthenticated session 401, app 200, unknown draft/mandate/history 404, pending step-ups 200 and logout 204.
2026-09-24 21:09 oskar1: added `python -m leash.api` and `docs/run-wallet.md`; launched it on 127.0.0.1:8128 against a temporary tree with runner #25 @fb57cfc, mandate routes #26 @636629e and app static @448a6d6, observed unauthenticated session 401, login 200 and `/app/` 200, then stopped the server.
2026-09-24 21:24 oskar1: after rebasing onto runner #25 @ed2bc3c and mandate #26 @636629e, launched `python -m leash.api` on 127.0.0.1:8129 with app static @448a6d6 in the temporary tree; observed `/session` 401 unauthenticated, login 200, `/app/` 200 and pending step-ups 200, then stopped the server.
2026-09-24 21:31 oskar1: rejected usernames containing internal spaces, tabs or newlines; one-off FastAPI TestClient smoke against app static @885d699 observed 422 for each and for whitespace-only input, and 200 for `local-demo`.
2026-09-24 18:39 policy_confirm: customer confirmation service and app routes; called `GET /drafts/{id}` and `POST /drafts/{id}/confirm` with FastAPI TestClient, observed 200 and mandate `mandate-1`, then 409 for stale confirmation; SCEN0002 merchant ambiguity raised `UnresolvedQuestion` before simulator call, @ae6380d.
2026-09-24 18:51 policy_confirm: reserved draft confirmation before simulator calls; one-off FastAPI call observed concurrent reject blocked, confirm 200 and stale repeat 409; simulated confirm failure raised and left pending simulator draft ID `sim-2` while revise was blocked, @7267153.
2026-09-24 18:46 policy_confirm: MCP authoring guide resource and two agent tools; called `list_tools`, `read_resource`, `get_policy_authoring_instructions`, and `propose_task_policy` through an in-memory MCP client, observed two tools, JSON guide and stored draft version 1; installed the source package with pip, @39b8e6e.
2026-09-24 19:39 oskar1: removed the unused server-side deterministic compiler and made the authoring guide category agnostic; stored a kitchen mixer proposal with one rule and observed two MCP tools through `list_tools`.
2026-09-24 20:32 oskar1: recorded the owner decision to defer global policy management until the runner loop, app step-up flow and replay work end to end; its later shape is in plan 04.
2026-09-24 23:52 policy_builder: steps 9 to 11 and the hosted transport. `get_policy_status` and `get_policy_summary` read the store only (`DraftStore.get_rejection`, `decision_state`), `buy` and `get_purchase_status` raise the contract message, and `--transport streamable-http --port` adds a `LEASH_MCP_TOKEN` bearer check. In-memory client: six tools listed; unknown draft, malformed draft id, `buy` and `get_purchase_status` returned their errors. One live SCEN0002 session recorded in `docs/samples/mcp-transcript-scen0002.md`: status `pending`, TestClient confirm 200 on the live simulator (draft `draft_e6f10b94a5428722`), then status `confirmed` with mandate `TM8b150c14b81c91d0`. Streamable HTTP on 127.0.0.1:8791 with the mcp client: with the header `list_tools` returned the six tools; without it and with a wrong token, 401. The same happened through a cloudflared quick tunnel with `--http-host-header`, where an unrewritten tunnel Host gets 421. Startup raised without the token or the port. `claude -p --mcp-config` with `scripts/leash_mcp_headers.sh` as `headersHelper` connected, listed six tools and got the unknown-draft error; `claude mcp add-json` stored only the URL and helper path. Step 3's evaluate-at-draft-time check is not implemented; `DraftStore` validates example shape only.
2026-09-25 00:12 david_main: host-side run on main 9d15742 with the demo shape. `uv run python -m leash.policy.mcp_server --transport streamable-http --port 8791` and `uv run python -m leash.api` on one temporary `LEASH_POLICY_STORE`, `cloudflared tunnel --url http://127.0.0.1:8791 --http-host-header 127.0.0.1:8791`, Claude Code registered with `claude mcp add-json leash-policy '{"type":"http","url":"https://<name>.trycloudflare.com/mcp","headersHelper":".../scripts/leash_mcp_headers.sh"}'` in a scratch `CLAUDE_CONFIG_DIR`; the stored config held zero token bytes. `claude mcp list` showed leash-policy connected. `claude -p` with the SCEN0002 instruction and `--allowedTools mcp__leash-policy__*` called `get_policy_authoring_instructions`, `propose_task_policy` (accepted first try), `get_policy_status` and `get_policy_summary` itself in 5 turns; the server log shows 22 POST 200 and no 421 or 5xx through the tunnel, a request without the header 401. Draft `b36dd191-7891-4582-8365-d0d4f4828367`, version 1, 5 rules (product type, size 43, MCC 5941, return_days >= 14, amount <= 200), 3 open questions, uncertainty `ask`; `GET /drafts/<id>` on the Wallet API returned the same draft after `/session` login and `/app/?draft_id=<id>` served 200. No confirm and no simulator call. Tunnel DNS took about a minute to resolve locally after cloudflared printed the name. Stack, token and scratch config removed afterwards.
2026-09-25 00:25 david_main: authoring guide gains the add-on instruction from Oskar's ruling (no `facts.is_addon = false` on a forbid-add-ons instruction). Dropped that rule from `docs/eval/replay-policies/SCEN0004.json` and rehashed it with `leash.policy.store.draft_hash`; the product_type wording stays literal as `docs/run-replay.md` states. Ran the full replay from `docs/run-replay.md` to /tmp: 45 attempts, 11 approve, 32 decline, 2 step_up, unchanged from `docs/eval/replay-2026-09-24-all.csv`, so the committed CSVs stand. With the wording matched to the catalogue in a scratch copy, the same policy gives AU0035 approve and AU0038, AU0042, AU0045 decline purchase_count_exceeded instead of four step_ups; that copy is not committed. `authoring_guide()["instructions"]` shows the new line.
2026-09-25 02:29 oskar4: added customer-owned GET /drafts and GET /mandates per proposal section (a)2; temporary FastAPI route smoke observed Alice's in-flight draft listed as confirming, Bob's list empty, unowned draft URL 200, empty mandate list 200, invalid filter 422. Ran the full offline replay: 45 rows, 11 approve, 32 decline, 2 step_up; the checked-in 2026-09-25 replay CSV is stale at AU0040: current main after PR #39 returns `item_mismatch|injected_instructions`, while the CSV retains `unrequested_item`. Code @8cee43d.
2026-09-25 03:05 oskar7: policy draft files now publish only after a complete fsynced write, preserving exclusive version creation. Ran `uv run python -c` with a temporary DraftStore and two concurrent revisions; observed one version 2, one DraftConflict, and readable versions 1 and 2. Topic branch lane/audit-session.
2026-09-25 03:15 oskar7: proposal validation now rejects period scope on unsupported numeric fields, numeric operators on text, incompatible list operators, currency on non-money fields and period_days without period scope. Ran `uv run python -c` against a temporary DraftStore: 3 valid drafts stored and 6 invalid drafts raised InvalidDraft with their reasons; no simulator call.
2026-09-25 02:49 oskar4: added section (b) customer global policy GET/PUT, canonical SHA-256/version writes, rule validation and generated display text; confirmation snapshots/composes global rules and mandate detail/list disclose the saved version/hash. Temporary FastAPI calls observed empty GET 200, PUT and readback 200, stale write 409, duplicate control 422. Offline replay from `docs/run-replay.md`: 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 on current main is `item_mismatch|injected_instructions`, differing from stale checked-in CSV as already recorded. No confirmation or simulator call. Runtime cross-mandate period enforcement remains gated on engine #49 plus runner caller wiring. Code @d39135b48f98ae843b5240600e8eece98b89b970.
2026-09-25 02:53 oskar4: rebased section (b) onto main after engine #49 merged; reran `docs/run-replay.md` against the 45 public attempts and observed 11 approve, 32 decline, 2 step_up, unchanged; AU0040 remains `item_mismatch|injected_instructions`. No API submission or customer resolution. Code @d39135b48f98ae843b5240600e8eece98b89b970.
2026-09-25 02:56 david_main ruling: section (b) route, persistence and confirmation composition may proceed with engine #49 on main; cross-mandate period enforcement becomes active when runner-intents passes `customer_mandates` to `state.load`. The policy PR body will disclose that runtime dependency. Current policy head remains based on PR #48 @e5f9e215ec0118990811d37c7fd3e5dbaf82f798.

2026-09-25 03:13 oskar4: rebased section (b) onto main after #48 and #52 merged at faff60e; global rules pass the same stricter `DraftStore._validate_rule` used for drafts. Temporary FastAPI calls observed v0 GET with canonical empty hash, valid period-rule PUT/readback 200 and generated display text, six unsupported combinations 422, duplicate 422, stale version 409, and customer isolation. Offline replay: 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 `item_mismatch|injected_instructions`. No simulator call or confirmation. Policy code @d39135b48f98ae843b5240600e8eece98b89b970.
2026-09-25 03:52 oskar4: bounded customer global-policy filenames by filesystem component limits while preserving existing quoted paths for short names; a temporary FastAPI store preserved alice version 3 and handled an 80-character Unicode username through login, GET v0, PUT v1 and read-back 200, with no simulator call, code @353ccc0.
2026-09-25 03:53 oskar4: reran `docs/run-replay.md` after the global-policy filename change; offline replay produced 45 attempts, 11 approve, 32 decline and 2 step_up, with AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-global-paths-replay.csv`, no API submission or customer resolution, code @353ccc0.
2026-09-25 03:56 oskar4: after rebasing the bounded global-policy filename change onto main 4bfb515, reran the explicit all-scenario command from `docs/run-replay.md`; 45 attempts, 11 approve, 32 decline and 2 step_up, AU0040 `item_mismatch|injected_instructions`, no simulator call or customer resolution, output `/private/tmp/policy-global-paths-rebased-replay.csv`, code @353ccc0.
2026-09-25 04:51 oskar4: implemented file-backed local accounts, sessions, wallet pairing, revocable MCP agent tokens, HTTP/stdio authorization, account-bound draft creation and owner checks on lane/policy-mcp-identity. Disposable FastAPI flow observed register 201 with cookie, duplicate 409, session 200/401, pairing GET 200 and approve 204, exact-Origin rejection 403, Referrer-Policy no-referrer, account-scoped agent list/revoke, and Alice-only proposed draft GET/list with Bob 404/empty list. In-memory MCP flow observed unauthenticated policy read denied, approved stdio pairing enabled it, second completion denied, and proposal created_for matched the approving account. No simulator call or confirmation. At this point, customer-lock integration and offline replay remained before the PR.
2026-09-25 04:56 oskar4: paired identity backend verified after rebase on runner #58 @eb78312. Account and session calls returned register 201 with cookie, duplicate 409, session 200/401, unknown-user failures 401 then 429, and logout 204 with cookie cleared. Wallet pairing GET/approve returned 200/204 with Referrer-Policy no-referrer and Origin denial 403; Alice-only draft GET/list and Bob 404/empty list passed. In-memory stdio MCP and a running streamable HTTP server both denied unpaired reads, enabled policy read only after one-time pairing, and created a proposal owned by the approving account. Global-policy GET v0 and PUT v1 passed through the customer and mandate lock helper. Full offline replay: 45 attempts, 11 approve, 32 decline, 2 step_up; AU0040 item_mismatch|injected_instructions. No simulator call or customer confirmation.
2026-09-25 04:58 oskar4: confirmation and global-policy PUT now acquire customer_lock first, lock sorted owned mandates, re-read the confirmation set and hold locks through the policy write, using runner #58 @eb78312 helpers. Disposable FastAPI call observed global-policy GET v0, locked PUT version 1 and readback version 1. Confirmation was not invoked and no simulator call was made. Offline replay from docs/run-replay.md: 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-mcp-identity-locks-replay.csv`.
2026-09-25 05:03 oskar4: applied Origin contract #75 @0a8b1b1 in the customer-lock follow-up. Registration/login and cookie-authenticated writes require exact LEASH_APP_ORIGIN; middleware checks cookie presence, leaving one session read/write in the route dependency. Disposable calls observed foreign Origin 403 and configured Origin 201/200 for register/login; pairing page Referrer-Policy remains set. MCP pairing and proposal descriptions state their phase order and that proposal does not buy anything. No simulator call or customer confirmation.
2026-09-25 05:08 oskar4: final follow-up head @aa5a9f1 passed the explicit docs/run-replay.md offline run: 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 item_mismatch|injected_instructions, output /private/tmp/policy-mcp-identity-locks-final-replay.csv. No live API, confirmation or invented answer.
2026-09-25 05:13 oskar4: expanded the existing MCP guide and begin_pairing/propose_task_policy descriptions to state pair, propose, Wallet confirm, verify confirmed status, search, authorize, and to say external browsing is outside backend control with no active agent purchase API. Offline docs/run-replay.md replay: 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 item_mismatch|injected_instructions, output /private/tmp/policy-mcp-identity-locks-guide-replay.csv.
2026-09-25 05:24 oskar4: mapped an owned confirmation-set change during customer lock acquisition to HTTP 409 on confirmation and global-policy PUT, with explicit detail, and documented both responses. `uv run python -m compileall -q src/leash/policy` completed; offline `docs/run-replay.md` replay produced 45 rows, 11 approve, 32 decline, 2 step_up, AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-lock-conflict-replay.csv`.
2026-09-25 05:28 oskar4: combined MCP phase guidance and owned-set conflict response verified at the post-#76 branch head. `uv run python -m compileall -q src/leash/policy` completed before the wording commit; final offline replay produced 45 rows, 11 approve, 32 decline, 2 step_up, AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-identity-conflict-final-replay.csv`.
2026-09-25 05:36 oskar4: POST /drafts/{id}/reject now holds the customer lock followed by sorted owned mandate locks through the rejection write, maps owned-set changes to 409 and lock timeout to 503; docs/contracts.md records the response. `uv run python -m compileall -q src/leash/policy` completed; offline replay produced 45 rows, 11 approve, 32 decline, 2 step_up, AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-reject-locks-replay.csv`. No live confirmation or simulator call.
2026-09-25 05:43 oskar4: documented HTTP 503 on lock-acquisition timeout for global-policy PUT, draft confirmation and draft rejection, matching the handlers. Docs-only validation: offline replay produced 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-lock-timeout-docs-replay.csv`.
2026-09-25 06:19 oskar4: added v2 canonical boundary-case hashing, explicit frozen-history evaluation at proposal/revision/confirmation, sidecar result snapshots, and the owner-safe boundary-results route. One-off disposable policy run produced approve, step_up and decline, rejected an expected/observed mismatch, observed proposal/revision/confirmation phases, Alice 200, Bob 404, stale results 409 and v1 empty results 200; legacy SCEN0002 v1 hash matched byte-for-byte. Offline `docs/run-replay.md` command produced 45 rows, 11 approve, 32 decline, 2 step_up; AU0040 `item_mismatch|injected_instructions`, output `/private/tmp/policy-boundaries-replay.csv`. No simulator, customer confirmation, or answer. Code @c5ef46d.
2026-09-25 06:34 oskar4: boundary-case revision now transitions v2 to v1 when cases are cleared, and sidecar readback checks observed and case_input_hash against each authored case; confirmation rereads the sidecar and rejects a changed snapshot before writing confirmation.json. Disposable store calls rejected altered observed, input hash and evaluated_at, with no confirmation write. Offline replay from docs/run-replay.md produced 45 attempts: 11 approve, 32 decline, 2 step_up, output /private/tmp/policy-boundary-revise-replay.csv. No live simulator call or customer confirmation. Code @0d51d5e.
2026-09-25 06:42 david_main: pairing link ruling, begin_pairing returns wallet_url from LEASH_APP_ORIGIN and the Pair screen accepts a typed code on the existing two pairing routes, docs/contracts.md only; oskar4 owns the MCP response, Rishabh the Pair screen field
2026-09-25 06:48 oskar4: MCP begin_pairing now returns wallet_url from the validated LEASH_APP_ORIGIN read at startup for stdio and streamable HTTP; no request Host or verifier enters the URL. Disposable real MCP clients called begin_pairing over both transports and observed a configured-origin URL with only the code. Missing origin failed startup on both transports, malformed origins failed server creation. Offline docs/run-replay.md replay produced 45 attempts: 11 approve, 32 decline, 2 step_up, output /private/tmp/policy-pairing-link-replay.csv. No Wallet approval or simulator call. Code @bc9001d.
