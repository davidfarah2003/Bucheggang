# 02 Decision engine

- Status: deterministic foundation and P1/P2 assessment contracts/composition merged. The real-provider exercise stopped on invalid Jev distributions; model-enabled runner integration and live customer verification remain incomplete.
- Owner: David (proposed)
- Lane: engine. Channel `team.zurichbuchegg.engine`, branch `lane/engine`, worktree `.worktrees/engine`
- User flow step: one proposed purchase in, `approve` / `decline` / `step_up` out, with evidence
- Design: [Decision pipeline](../idea/viseca-agent-control-layer.md#decision-pipeline), [Security requirements](../idea/viseca-agent-control-layer.md#security-requirements)
- Papers: [SAFR](../papers/SAFR.pdf) (checkpoint between proposal and execution, every action re-evaluated, audit record fields), [Verifiable Intent security model](https://verifiableintent.dev/spec/security-model/) (budgets and occurrence limits need state; signatures alone cannot enforce them), [AP2 specification](https://ap2-protocol.org/ap2/specification/) (approval bound to the exact purchase)
- Challenge API: [technical_details.md, steps 6 to 8 and Rule format](../../viseca-2026/technical_details.md); [event schema](../../viseca-2026/data/schemas/authorization_event.schema.json)
- Contracts: `Event`, `MandateState`, `Check`, `Decision`, reason codes, engine entry points in [contracts.md](../contracts.md). This lane owns `src/leash/contracts/`.

## Goal

A pure function, `evaluate(event, policy, state, facts, assessments=None) -> Decision`. No network or model calls, with a target under 50 ms. Every explicit requirement (amount, period budget, purchase count, expiry, allowed categories) is decided here. Completed model assessments may add pass or uncertain checks; they never satisfy a missing required fact or overturn a deterministic failure. The runner calls the function for live purchases and `scripts/replay.py` calls it for the 45 offline attempts. The confirmation screen's prose examples remain illustrative.

## Scope

In:

- The contracts package: strict pydantic models mirroring the organizer schema.
- Rule evaluation over the field vocabulary, with `scope: purchase` and `scope: period` plus `period_days`.
- Mandate state per `mandate_id`: accepted approvals by simulated timestamp, handled live authorization IDs with their accepted result, pending step-ups, declines. Persisted to `data/state/<mandate_id>.json` after every accepted result.
- Checks beyond the rules, each returning a `Check`: every cart line against the requested item; unrequested add-ons; return terms; merchant category and type; merchant and device familiarity from `authorization_history.csv` (the base pack covers all four scenario cards); velocity from `recent_attempt_count_10m`; country; duplicate orders and re-quotes via `related_authorization_id`; gift cards, subscriptions, protection plans; injected instructions reported by the extract lane; lookalike merchant (name close to a known merchant, different `merchant_id`).
- Mapping uncertainty to the mandate's `uncertainty_policy` (`ask` becomes `step_up`).
- The `Decision` record with reason codes, evidence, a one-paragraph explanation, `engine_version`, elapsed time and the mandate version used.

Out:

- Reading merchant text (plan 03). The engine consumes `PurchaseFacts`; unknown facts count as uncertain.
- HTTP, polling, deadlines (plan 05).
- The confirmation flow (plan 01).

## Steps

1. Contracts package. Load `example_authorization_request.json` through it once to see it parse.
2. Rule evaluator: resolve a field path against the event, facts, history and state. Operators `<`, `<=`, `=`, `!=`, `>`, `>=`, `in`, `not_in`. Period rules add the trailing-window approvals (simulated timestamps) to the purchase amount before comparing. Unknown value gives `uncertain`, never `pass`.
3. State store with idempotent `record`. Only an API-accepted `approve` moves spend and count. A pending `step_up` moves nothing. Delivery fees are already inside `billing_amount_chf`; do not add them twice.
4. Built-in checks, each a small function, in this order: mandate active and not expired; hard rules; cart lines vs requested item; add-ons; return terms; merchant type; familiarity; velocity; country; duplicate and re-quote; gift card, subscription, protection plan; lookalike merchant; injected instructions.
5. Combine: any `fail` on an explicit rule or a hard check gives `decline`. Any `uncertain` with no `fail` gives the `uncertainty_policy` outcome. All `pass` gives `approve`. Injected instructions never change a rule: they add a reason code and evidence, and mark that line's facts uncertain.
6. Explanation and customer message in the customer's own terms ("CHF 230 is over your CHF 200 limit"). Evidence lists every check with value and source.
7. Time the replay of the 45 attempts once and write p99 in the Log. Target under 50 ms.

## How we check it works

- `scripts/replay.py --all` with the five explicit policies in [run-replay.md](../run-replay.md) runs every public attempt. Compare with `docs/eval/labels.csv` after human reconciliation; label agreement is unavailable before then. The report lists every action disagreement.
- Feeding the same live ID twice changes nothing the second time (try it once by hand).
- Rolling spend: two approvals then a third over a 7-day limit is declined; a declined purchase in between does not count (try it once in the replay).
- `grep -rn "SCEN00\|AU00\|replay_order" src/leash/engine/` prints nothing.

## Decisions

- For "a shop I use regularly", an unseen merchant fails when the confirmed instruction explicitly requires prior use. If it does not, merchant familiarity is uncertain.
- AU0016 has unknown return terms, so it is uncertain and follows the confirmed draft's uncertainty policy. If the customer answered the return-terms question during confirmation, evaluate against the rule created from that answer.
- Treat the two SCEN0001 purchases minutes apart as separate orders for the per-order limit. Count both against the period limit and explain this interpretation on the judge panel.
- A card with no row in `authorization_history.csv` before the purchase and no approval recorded on the mandate has no basis for device, merchant or country familiarity. The familiarity check is uncertain with the one reason code `no_card_history` and one message, and the country check is not reported separately. A customer rule on `history.*` is uncertain for such a card, with the same code. A card with history keeps the device, merchant and country checks. The live simulator serves the pack's history file unchanged, so the live card CA1331 is such a card (docs/eval/live-SCEN0101-2026-09-24.md on lane/runner). Owner ruling, 2026-09-24.
- The built-in country check reports `country_unfamiliar` when it is uncertain. `country_blocked` is used only when a customer rule on `authorization.merchant.merchant_country` fails.

## Log

(one line per finished task: date time, who, what, how it was tried, sha)
2026-09-24 19:47 engine_builder: task 1 contracts package src/leash/contracts/ (Event strict mirror of the organizer schema, PolicyDraft, Rule, Mandate, PurchaseFacts, MandateState, Check, Decision, StepUp, StepUpAnswer); `uv run python -c "Event.model_validate(json.load(open('viseca-2026/data/scenario_fixtures/example_authorization_request.json')))"` printed `AU_EXAMPLE_0001 20.0 1`; docs/samples/scen0002_draft.json parses as PolicyDraft; an extra key plus a string amount raise 2 validation errors. Unknown facts are `None` in PurchaseFacts. @a6ec5bf
2026-09-24 19:51 engine_builder: task 2 rule evaluator leash.engine.rules (evaluate_rule, evaluate_rules, reason_code; history familiarity from authorization_history.csv; period scope sums accepted approvals in the trailing window of simulated time). Tried with the 12 SCEN0002 attempts, the sample draft and leash.extract facts: AU0012 all pass, AU0013 size 42 fail, AU0014/AU0015 return days 0/7 fail, AU0016 return days unknown gives uncertain, AU0017 trail shoes fail, AU0021 CHF 215 fail, AU0022 sustainable_goods fail, approvals_count fails after the first approval; under 0.1 ms per attempt. A 7-day CHF 400 period rule passed at 150+165 and failed at 250+165 with an 8-day-old approval excluded. Branch lane/engine-rules, stacked on PR #15. @602e280
2026-09-24 20:20 engine_builder: tasks 3 to 5. Added leash.engine.state: load, a pure apply, and record(mandate_id, event, accepted) -> MandateState, which writes data/state/<mandate_id>.json, is idempotent and allows one move from step_up. Added leash.engine.checks with the 13 checks in plan order, and leash.engine.evaluate. Tried the sample draft docs/samples/scen0002_draft.json over the 12 SCEN0002 attempts with leash.extract facts. In sequence, AU0012 approves and the one-pair rule declines every later attempt. On fresh state per attempt: AU0012, AU0019 and AU0023 approve, AU0016 steps up with return_terms_missing, and the rest decline. Max 0.6 ms. Tried record: the same decision twice is a no-op, step_up then decline clears pending, and decline then approve raises StateConflict. facts=None on AU0012 gives step_up. Ad hoc drafts for SCEN0001/3/4 made period_limit_exceeded, new_device, unfamiliar_merchant, lookalike_merchant (PixelHarbour), duplicate_order, gift_card and injected_instructions fire. An unseen merchant without a prior-use rule passes familiarity with a note; PR #17 says uncertain, which would make AU0023 a step_up against the catalogue's "unfamiliar but fully compliant seller". Owner to confirm. @9098516
2026-09-24 20:32 engine_builder: rebased on main after #21 and #20. PurchaseFacts sources are now agent_form | structured | merchant_text, and structured maps to Check source event. Owner ruling applied to the familiarity check. The SCEN0002 instruction asks for a "specialist sports retailer" and does not ask for "a shop I use regularly", so an unseen merchant is not a fail there. Familiarity fails only through a customer rule that requires prior use (history.merchant_seen_on_card). Otherwise an unseen merchant or device makes the check uncertain and the customer's "ask me when uncertain" applies. Reran SCEN0002 on fresh state: AU0023 (Summit Thread, never used by this card) is now step_up unfamiliar_merchant. AU0012 and AU0019 approve, AU0016 steps up, and the rest decline as before. In sequence, AU0012 approves and the rest decline. Max 0.45 ms. @dcbbfd7
2026-09-24 20:22 engine_builder: task 6. leash.engine.words writes the customer_message and explanation. A failed or uncertain rule is quoted in the customer's words (source_text) next to what this purchase shows, and built-in checks use their note. The explanation lists the failed checks, the unchecked ones, the uncertainty setting and a count of passed checks. Tried on SCEN0002 with fresh state: AU0021 "Declined CHF 215.00 at TrailSpark. You said "pay no more than CHF 200", and this order is CHF 215.00, over your CHF 200 limit."; AU0016 "Please confirm CHF 175.00 at TrailSpark. You said "returned within 14 days or more", and the seller does not state a return window."; AU0013 "... You said "size 43", and the item is size 42." A period rule reads "... would bring your spending over the last 7 days to CHF 361.50, over your CHF 300 limit". @2a4bf3c
2026-09-24 22:05 engine_builder: owner ruling on cards with no history, and task 7. A card with no row in authorization_history.csv before the purchase and no approval on the mandate now gives one uncertain familiarity check with reason no_card_history, the country check is left out, and a customer history.* rule is uncertain with the same code. The uncertain country check now reports country_unfamiliar; country_blocked stays for a failed country rule. Both codes are in docs/contracts.md and leash.contracts.decision. Tried with the pack's AU0002 rebuilt on the live card CA1331 and device DVC-B81C42, amount-only draft: step_up no_card_history, one non-passing check of 15, "Please confirm CHF 44.50 at Alpine Basket. This card has no purchase history yet, so we cannot tell whether this shop, this device or a shop in CH is familiar to you." The same card under the SCEN0000 and SCEN0003 prior-use rules gives step_up no_card_history for each first purchase. AU0029 on its own card (history, never GB) still reports country uncertain as country_unfamiliar. Task 7: a throwaway loop (not committed) built the 45 events from purchase_attempts.csv, merchants.csv and purchase_attempt_items.csv, ran leash.extract.extract_event, then evaluate in replay order per scenario with state carried by state.apply, using docs/samples/scen0002_draft.json for SCEN0002 and ad hoc drafts for SCEN0000, 0001, 0003 and 0004. evaluate alone, 20 passes after a warm-up, n=900: p50 0.19 ms, p99 0.25 ms, max 0.28 ms, well under the 50 ms target. The pack's 45 decisions are unchanged by the ruling since all four scenario cards have history. @f5c1fb3
2026-09-25 01:10 david_main: engine history reads both organizer packs. `leash.engine.data.History` takes every `authorization_history.csv` given and `HISTORY` loads `viseca-2026/data/` (41 public cards, 4,701 rows) plus `viseca-2026/additional-data-history/` (832 cards, 144,674 rows, disjoint authorization ids; a duplicate id raises). Cause: the live team card CA1331 has 0 rows in the base pack and 173 in the additional pack, so every live purchase reported no_card_history and device, merchant and country familiarity stayed uncertain (live-SCEN0101 Log 21:44, david_orch's HistoryIndex check 00:45). Checked: `HISTORY.card_rows("CA1331", now)` 173 and 20 known merchants; CA0001 unchanged at 122; import of leash.engine.data 0.55 s. Full replay from docs/run-replay.md: 45 attempts, 11 approve, 32 decline, 2 step_up, every row equal to a replay of plain main 490c64f (AU0040 lost unrequested_item at #39, before this change). @359b5cb
2026-09-25 02:45 david_main: MandateState.customer_approvals for cross-mandate period spend (proposal docs/proposals/policy-ui-backend-contract.md section b, requested by oskar4 on contracts). state.load(mandate_id, customer_mandates=()) fills it from the named mandates' files, record and _save drop it, _in_window counts it; evaluate signature unchanged. Tried: mandate A approves AU0012 (CHF 165), mandate B alone approves AU0019 (CHF 168) under a 7-day CHF 300 period rule, B loaded with A declines period_limit_exceeded "CHF 165.00 approved in 7 days + CHF 168.00 now: 333.0"; B's saved file has customer_approvals []; a per-mandate approvals_count rule still approves on B; the same authorization approved on two mandates raises StateConflict; full replay 45 rows 11/32/2 identical to main in decision and reason codes. Branch lane/engine-customer-approvals.

2026-09-25 01:31 david_orch: P1/P2 shared assessment contracts and pure model-check composition @2b0486b, isolated and held from main. Four actual TypeSafe-only Jev calls served typesafe/jev-1.13-20260917 in 539/295/453/481 ms. Public AU0001 stayed approve; AU0037 retained its hard decline despite model passes; live AU10319 retained its hard decline with two uncertain model checks; live AU10234 retained step_up despite model passes. No simulator submit or customer answer. Nine altered copies of a captured real bundle raised on identity, amount, policy, time, state or probability defects; expired adapter budget raised before HTTP. All 45 public attempts with real history-only bundles matched model-off decisions, reasons and evidence at 11 approve/32 decline/2 step_up. Normal replay exited 0, local total p50 0.235 ms, p99/max 0.415 ms. After rebasing onto #45 merge 0b7c2c1, captured-response recomposition kept all four outcomes and removed no_card_history from AU10234 under both configurations. Evidence and captured distributions: docs/eval/classifier-composition-2026-09-25.md and .json. Behavioural artifact, threshold, live runner composition and P3/P4 remain open; the noon/model-enabled hold is unchanged.

2026-09-25 02:02 david_orch: engine-owner digest review extended the shared helper to assessment_purchase_digest(event, features), matching the producer payload including all feature values, missingness and support. All 45 current-policy history bundles matched the producer digest and model-off decisions/reasons/evidence. Re-composed the four captured real Jev responses with their original profiles plus the reviewed evaluation-only CatBoost artifact e4815c5677073c4e985c56f09f045be3d72c4e3ae624b75f9d06870630c828cf: all flags false and outcomes unchanged. Re-ran ten refusal cases against the migrated valid bundle; changing a finite feature value specifically failed the purchase-digest check. No new provider or simulator call, no threshold or customer answer. The challenge-key exposure report blocks live simulator verification pending human rotation.

2026-09-25 david_orch: rebased held P1/P2 onto main5bc208d after #49 customer_approvals and #48 list routes. Preserved both contract interfaces and both logs; classifier.py, model_checks.py and evaluate.py byte-identical to reviewed6518cf8. Replay on e5b3ceb returned45 rows11/32/2, p50 total0.233ms p99/max0.421ms. Four captured genuine Jev/artifact bundles recomposed to approve/decline/decline/step_up with three model checks each; altered feature content refused on all four. First probe stopped on differing capture metadata; corrected inline probe completed, no source or capture changes. No remote call. New exact-head review required; both holds remain.

2026-09-25 david_orch: real Jev exercise on core71f119c and standalone producer0940fd7 with evaluation-only CatBoost e4815c5. Initial driver exited1 after6 composed inputs when request7 failed activity probability normalization; one separate diagnostic request passed the unchanged parser. Fresh recorded driver exited1 after35 composed inputs when request36 returned spend probabilities unusual0.14, ordinary0.80 and unclear0.05, sum0.99. Total44 actual provider requests; neither45-input batch completed. No retries within batches, normalized probabilities, substitute decisions, simulator requests or customer answers. Full model-off scripts/replay.py --all still returned45 rows11/32/2, local p50 total0.239ms p99/max0.421ms. Evidence: docs/eval/jev-public-2026-09-25.md and linked partial CSVs/JSON. Strict validation and the model-enabled release gate remain.
2026-09-25 05:52 david_main: boundary-case contract on lane/contracts-boundary-history: PolicyDraft.hash_version (1 default, 2 with boundary_cases), BoundaryCase (complete Event, one PurchaseFacts per item, MandateState, frozen History), History {authorizations: [HistoryAuthorization]}, PurchaseFacts.conflicts {field, kind} with kinds merchant_text_contradiction and catalogue_event_mismatch, reason code fact_conflict, evaluate(..., history=None). Tried: legacy scen0002_draft parses with hash_version 1; v2 without cases, a conflict with a value, a duplicate history id and a state for another mandate each raise; compileall clean; replay --all to /tmp/replay-boundary.csv 45 rows 11/32/2, decision column identical to docs/eval/replay-2026-09-25-all.csv (the CSV's AU0040 reason string still carries unrequested_item from before 23508d3; the engine is untouched here). Contract only, no engine, store or extractor behaviour changes.
2026-09-25 06:12 david_main: engine slice of #84 on lane/engine-history-conflicts: evaluate(..., history=None) builds a History index from an explicit contracts History slice (data.History.from_rows) or keeps the packaged CSVs; a facts.* rule reading a field named in PurchaseFacts.conflicts is uncertain with fact_conflict added, and under uncertainty_policy approve the outcome is step_up, never approve; engine version 0.6. Tried: replay --all 45 rows 11/32/2 decision column identical to docs/eval/replay-2026-09-25-all.csv; AU0012 (SCEN0002, approve within_policy) with return_days marked merchant_text_contradiction gives step_up [return_terms_missing, fact_conflict] under approve, step_up under ask, decline under decline, while a plain unknown under approve still approves; explicit empty History makes familiarity uncertain no_card_history and a one-row slice with the merchant and device makes it pass. Also records the GET /drafts/{draft_id}/boundary-results readback ruling in docs/contracts.md.
